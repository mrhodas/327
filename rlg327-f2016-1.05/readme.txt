Name: Michael Rhodas
Class: Com S 327
Assignment: 5

-This version does not work, just shows attempt of converting to .cpp
- This program renders in ncurses. It will pause for your input at each step.
- < and > render new maps with monsters
- m brings up list but does not scoll, escape will work though, hit it twice.
- There is now fog that only renders what the PC sees. 